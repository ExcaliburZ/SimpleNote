<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionModeBackground">@color/colorPrimary</item>
        <item name="android:actionBarStyle">@style/Widget.ActionMode.customer</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Base.TextAppearance.AppCompat.Date" />

    <style name="Base.TextAppearance.AppCompat.Title.Note" />

    <style name="Widget.ActionMode.customer" parent="Widget.AppCompat.ActionMode">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:height">?android:attr/actionBarSize</item>
        <item name="android:actionModeBackground">@color/colorPrimary</item>
    </style>


</resources>
